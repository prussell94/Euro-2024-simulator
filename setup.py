from setuptools import setup, find_packages

setup(
    name='Euro-2024-simulator',  # name of the module
    version='0.1.1',
    packages=find_packages(),  # automatically finds and includes your packages
    install_requires=[],  # list your dependencies here, if any
)